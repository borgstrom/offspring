language: python
sudo: false

branches:
  only:
    - master

python:
  - 2.7
  - 3.5
  - 3.6
  - pypy

install:
  - pip install --upgrade codecov pytest pytest-mock pytest-cov
  - pip install -e .

script:
  - py.test -v --cov=./src tests/

after_success:
  - bash <(curl -s https://codecov.io/bash)
